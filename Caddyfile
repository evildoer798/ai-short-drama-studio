(app_proxy) {
  encode zstd gzip
  request_body {
    max_size 512MB
  }
  reverse_proxy web:14000
  header {
    X-Content-Type-Options nosniff
    Referrer-Policy strict-origin-when-cross-origin
    -Server
  }
}

http://{$SERVER_IP} {
  import app_proxy
}

{$APP_DOMAIN} {
  import app_proxy
}
